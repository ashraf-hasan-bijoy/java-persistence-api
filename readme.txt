Build the project using "gradle clean fatJar"
Run the project using "java -jar build/libs/java-persistence-api-all-1.0.jar"
Log file "debug.log"  will be in /tmp/jpa-app directory
To run a code related to specific topic uncomment the processor call in Main.java class

